pipeline{
agent any
  stages{
    stage(build){
      steps{
      sh 'echo "building the code" '
      }
      }
     stage(deploy) {
      steps{
      sh 'echo "deploying the code" '
      }
     }
  }
}
